# ── Training Dependencies (Google Colab) ─────────────────────────────────────
# Used inside Training_Notebook.ipynb and Inference_Notebook.ipynb.
#
# !! Do NOT pip install this file directly !!
# The install cell at the top of each notebook handles the correct install
# order — Unsloth must be installed first as it patches torch internals.
#
# This file documents versions for reference and for non-Colab GPU setups.
# Versions sourced from unsloth's pyproject.toml (checked April 2026).

# ── Unsloth ───────────────────────────────────────────────────────────────────
# Installed via:  pip install unsloth
# Torch is a dependency of unsloth and will be installed automatically
# at the correct version for your CUDA environment.

# ── Core ML ───────────────────────────────────────────────────────────────────
transformers>=4.51.3,<=5.5.0   # excludes several broken releases (see unsloth pyproject.toml)
trl>=0.18.2,<=0.24.0           # SFTTrainer for supervised fine-tuning
peft>=0.18.0                   # LoRA / QLoRA adapter support
accelerate>=0.34.1             # mixed-precision training utils
bitsandbytes>=0.45.5           # 4-bit / 8-bit quantisation (excludes 0.46.0 and 0.48.0)

# ── Dataset handling ──────────────────────────────────────────────────────────
datasets>=3.0

# ── Experiment tracking ───────────────────────────────────────────────────────
wandb>=0.18

# ── Evaluation ────────────────────────────────────────────────────────────────
codebleu>=0.6
sentence-transformers>=3.0
Pillow>=10.0

# ── Visualisation (used in generated chart code) ──────────────────────────────
matplotlib
